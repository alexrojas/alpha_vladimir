
<div class="col-xs-8 col-xs-offset-2">
  <div class="alert-danger">
        <% if obj.errors.any? %>
        <h2 class="panel-title"> <%= pluralize(obj.errors.count, "error ") %>prohibited this article from being saved!</h2>
        <ul>
          <% obj.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
          <% end %>
        </ul>
        <% end %>
  </div>
</div>
